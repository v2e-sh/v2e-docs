# Infrastructure audit — 2026-07-03

> Automated nightly audit (multi-agent, official-docs-referenced, adversarially verified). Every finding below was confirmed at a real `file:line` by an independent verifier; false positives and homelab-overkill items were dropped. `NEW` = not already in the HANDOVER/SOLIDIFY backlog or the prior external review.

**36 findings** — 2 high, 12 medium, 18 low, 4 nit · 30 new.

!!! warning "Scope"
    This is analysis only — no changes were deployed. Findings are proposals for review. Severity is calibrated to a single-operator homelab.


## High

### Arcane mounts docker.sock read-write (root-equivalent on the services host) · _tracked_
- **Where:** `v2e-compose/arcane/compose.yml:27`
- **Problem:** Full RW `/var/run/docker.sock` = root on the host. Already tracked as H2 (HANDOVER §5, SOLIDIFY 'Docker security'). Confirmed here at file:line; the upstream Tecnativa docker-socket-proxy pattern (getarcane.app/docs/setup/socket-proxy) is the fix and is referenced inline in the compose comment.
- **Official reference:** Arcane docs 'Socket Proxy Setup' (getarcane.app/docs/setup/socket-proxy): front the socket with Tecnativa docker-socket-proxy exposing only the needed API endpoints.
- **Fix:** Add a docker-socket-proxy sidecar on an internal network and point Arcane at `DOCKER_HOST=tcp://docker-socket-proxy:2375` (per H2).
- **Verifier:** CONFIRMED at /Users/alex/Documents/v2e-environment/v2e-compose/arcane/compose.yml:27 — `- /var/run/docker.sock:/var/run/docker.sock` is a full read-write socket mount (no `:ro`, and docker.sock is RW-only anyway). This is root-equivalent on the services host, exposed to a Traefik-fronted web service (Arcane), so the high severity is justified. Not already fixed: no docker-socket-proxy sidecar exis…

### sops + age binaries baked into every VM are downloaded with NO checksum/signature verification · _tracked_
- **Where:** `v2e-templates/lib/build-template.sh:44-49`
- **Problem:** Every build fetches sops v${SOPS_VERSION} and the age tarball straight from GitHub over the network and copies them into /usr/local/bin of every node image (control, services, agent, infra) with no integrity check. The age tarball (lines 47-49) is streamed directly into `tar -xz`, which structurally prevents any pre-extraction verification. A compromised/hijacked GitHub release, a CDN/MITM, or a typo'd URL becomes root-level code execution on every VM in the estate. This is the single unverified supply-chain path into the whole fleet, and it runs on every clean rebuild.
- **Official reference:** getsops v3.9.4 publishes sops-v3.9.4.checksums.txt signed with Cosign (GitHub OIDC) + SLSA provenance (sops-v3.9.4.intoto.jsonl) + SPDX SBOM (confirmed on the release page); SLSA/supply-chain guidance requires verifying a checksum/signature before executing a downloaded binary. Same TOFU-pinned-sha256 pattern the repo already uses for cloud images applies here.
- **Fix:** Add SOPS_SHA256 and AGE_SHA256 to config.env and verify with `sha256sum -c` before `--copy-in` (mirror the image path at lines 26/34); download the age tarball to a file first, verify, then extract. Optionally verify the cosign-signed sops checksums file.
- **Verifier:** CONFIRMED at /Users/alex/Documents/v2e-environment/v2e-templates/lib/build-template.sh:44-49. Line 45-46 curl the sops binary and lines 47-49 stream the age tarball straight into `tar -xz -C "$work"` with zero integrity check; both are then --copy-in'd to /usr/local/bin and chmod 0755 (lines 65-68). No SOPS_SHA256/AGE_SHA256 exists anywhere — config.env (lines 11-12) defines only SOPS_VERSION="3.9…


## Medium

### Router SSH keeps password authentication enabled while the bootstrap 'vyos' user retains default vyos/vyos · **NEW**
- **Where:** `v2e-tf/cloud-init/vyos-router.yaml.tftpl:29 (`set service ssh port '22'`); router_password_hash default "" in variables.tf:166-176`
- **Problem:** The router configures `service ssh` but never sets `disable-password-authentication`, so VyOS accepts SSH password auth. All three admin keys (mac + both mesh keys) are already provisioned to the vyos user, so password auth is pure attack surface. When router_password_hash is left blank (the default) the image-default vyos/vyos password stays active, so any host on the control VLAN (forward rule 10) — or a trusted_mgmt WAN source (input rule 30) — can log in as vyos/vyos over SSH. Not covered by HANDOVER M1-M5.
- **Official reference:** VyOS SSH hardening (docs.vyos.io — `set service ssh disable-password-authentication`) recommends key-only auth once keys are deployed; the repo already enforces this posture on the Linux nodes (ssh_pwauth: false) but not on the router.
- **Fix:** Add `- set service ssh disable-password-authentication` to the vyos_config_commands (keys are guaranteed present), and/or require router_password_hash to be non-default. Keeps key-based Ansible bootstrap working while closing the default-cred path.
- **Verifier:** CONFIRMED at real lines. vyos-router.yaml.tftpl:29 is `- set service ssh port '22'` (SSH configured); grep confirms NO `disable-password-authentication` anywhere in v2e-tf/, so VyOS accepts SSH password auth. router_password_hash default "" at variables.tf:166-176, and the variable's own description states "Empty = key-only (console fallback is the image default vyos/vyos)" — confirming default vy…

### requirements.yml pins some collections but not the ones the roles actually import (community.general, community.crypto, vyos.vyos, ansible.netcommon) · **NEW**
- **Where:** `v2e-ansible/requirements.yml:12-23`
- **Problem:** The playbooks use community.general.sudoers + community.general.npm (roles/ai_identities/tasks/main.yml, roles/ai_workbench/tasks/main.yml), community.crypto.openssh_keypair (roles/ai_identities/tasks/main.yml), vyos.vyos.vyos_config (roles/killswitch/*, roles/vyos_hardening_basic/tasks/main.yml) and the ansible.netcommon.network_cli connection + vyos.vyos network_os (inventory/hosts.ini vyos:vars). None of these four collections are declared/pinned in requirements.yml — only community.docker, devsec.hardening, community.sops and ansible.posix are. On control they are resolved implicitly from whatever version the pipx `ansible` bundle ships, so their versions float even though `ansible-galaxy collection install -r requirements.yml` pins the others. This violates the owner's pin-exact rule and is internally inconsistent (ansible.posix is pinned with a comment 'pin it so it's explicit + Renovate-tracked' while the equally-used vyos.vyos/community.general are not).
- **Official reference:** Ansible Galaxy user guide — 'Installing roles and collections from the same requirements.yml file' / requirements.yml recommends pinning every directly-used collection with an explicit version so runs are reproducible; the repo's own stated rule (pin exact, Renovate-bumped).
- **Fix:** Add explicit version pins for community.general, community.crypto, vyos.vyos and ansible.netcommon to the collections block in requirements.yml (matching the versions currently shipped by the target ansible bundle), so a clean rebuild installs the same versions the playbooks were tested against and Renovate tracks them.
- **Verifier:** Confirmed at /Users/alex/Documents/v2e-environment/v2e-ansible/requirements.yml. The collections block (lines 11-23) pins exactly four: community.docker (5.2.0), devsec.hardening (10.6.0), community.sops (2.3.0), ansible.posix (2.2.0). None of the four flagged collections are declared:  - community.general: used at roles/ai_workbench/tasks/main.yml:44 (community.general.npm) and roles/ai_identitie…

### Unattended-rebuild time-bomb: ansible is installed unpinned (latest) and relies on allow_broken_conditionals, which ansible-core removes in 2.23 · **NEW**
- **Where:** `v2e-ansible/ansible.cfg:15 (allow_broken_conditionals = True) + v2e-tf/cloud-init/node.yaml.tftpl:183 (pipx install ansible, ansible_version defaults empty)`
- **Problem:** ansible.cfg sets allow_broken_conditionals=True purely to tolerate artis3n.tailscale v5.0.1's string-valued `when:`. Per the ansible-core 2.19 porting guide this is a temporary escape hatch that 'will become an error in ansible-core 2.23'. The control node installs ansible via `pipx install ansible` with ansible_version defaulting to empty = latest (v2e-tf variable description itself says 'Empty = latest at first boot (not reproducible)'). So a clean rebuild done after ansible-core 2.23 ships will hard-fail the tailscale phase even with the config flag set — the 'unattended clean-rebuild must keep working' guarantee has a shelf life nobody controls. The artis3n role is also already archived (Nov 2025) with its collection successor available.
- **Official reference:** Ansible-core 2.19 Porting Guide: broken conditionals — 'This warning will become an error in ansible-core 2.23'; artis3n has migrated the standalone role into the artis3n.tailscale collection (standalone repo archived).
- **Fix:** Either pin ansible_version in the tfvars/cloud-init to a known-good ansible release (< the 2.23 line) as a stopgap, or migrate roles/tailscale off the archived standalone role onto the artis3n.tailscale collection (whose conditionals are fixed) and then drop allow_broken_conditionals entirely. Pinning the install closes the reproducibility hole regardless.
- **Verifier:** VERIFIED, stands (medium) — with one caveat on the official reference precision.  Repo claims all TRUE and at the cited lines, not already fixed: - /Users/alex/Documents/v2e-environment/v2e-ansible/ansible.cfg:15 — `allow_broken_conditionals = True`, present. The surrounding comment (lines 12-14) confirms the exact rationale the finding states: "artis3n.tailscale 5.0.1's final 'role migrated' noti…

### All SOPS secrets are delivered globally (-e @secrets) and env.j2 renders every secret on every compose host, so the infra node persists the Cloudflare token + all app secrets it never uses · **NEW**
- **Where:** `roles/compose_stack/templates/env.j2 (renders CF_DNS_API_TOKEN, GRAFANA_ADMIN_PASSWORD, SEMAPHORE_*, ARCANE_* unconditionally) + v2e-tf/cloud-init/node.yaml.tftpl:191 (ansible-playbook ... -e @$HOME/.v2e-secrets.yml)`
- **Problem:** Secrets are shipped as `-e @~/.v2e-secrets.yml` (whole decrypted SOPS file as extra-vars = global, highest precedence, in scope for every host/play), and env.j2 is the same template on services and infra. It emits every secret key with default('') regardless of node role, so the infra appliance's /opt/v2e-compose/.env (0640 root:docker on disk) ends up containing the Cloudflare DNS-01 token, Grafana/Semaphore/Arcane secrets — none of which infra runs (it only needs technitium_admin_password, mullvad_* and tailscale_authkey). The per-group scoping that landed earlier is only the `compose_stack_required_secrets` assert (a presence gate), not the actual value distribution. Blast radius: a compromise of the infra node (mgmt-VLAN, host-network Technitium/RustDesk) yields the services-node app secrets and the broad CF token.
- **Official reference:** Ansible docs — variable precedence & 'Keeping vaulted variables safely visible' / SOPS + community.sops vars-plugin design: scope secrets to the group that needs them (group_vars/<group>) rather than group_vars/all or global extra-vars, to minimize secret exposure per host.
- **Fix:** Split the SOPS secrets into per-group files (e.g. group_vars/services.sops.yaml vs group_vars/infra.sops.yaml) loaded by the community.sops vars plugin instead of a single `-e @all-secrets`, and/or make env.j2 conditionally omit secret lines a given host group doesn't consume, so each node's .env holds only its own secrets.
- **Verifier:** VERIFIED — all factual claims check out against the actual repo.  file:line confirmed: - v2e-ansible/roles/compose_stack/templates/env.j2:10,14-20 — CF_DNS_API_TOKEN, SEMAPHORE_DB_PASS/ADMIN_PASSWORD/ACCESS_KEY_ENCRYPTION, ARCANE_ENCRYPTION_KEY/JWT_SECRET, GRAFANA_ADMIN_PASSWORD, SEMAPHORE_RUNNER_REG_TOKEN all rendered unconditionally with default(''). TRUE. - v2e-tf/cloud-init/node.yaml.tftpl:191…

### Loki, Grafana and Alloy have no Prometheus scrape job — the compose comment claiming Loki readiness is scraped is false, so a dead log pipeline fires no alert · **NEW**
- **Where:** `v2e-compose/observability/config/prometheus.yml:14-27 (scrape_configs) vs compose.yml:80-81 (loki comment)`
- **Problem:** prometheus.yml only defines jobs for prometheus, node, cadvisor, traefik (+ blackbox-cert/blackbox-dns on the alerting branch). There is NO scrape job for loki (:3100/metrics or /ready), grafana (:3000/metrics) or alloy (:12345/metrics). Yet compose.yml:80-81 documents Loki's missing healthcheck as acceptable because 'Readiness is scraped by prometheus + alerted via grafana instead (GET /ready)'. That scrape does not exist. The only failure alert is 'target-down' (count(up==0)), and since Loki/Grafana/Alloy are not targets, a Loki crash-loop silently stops all log ingestion with zero alerting — exactly the silent-failure mode the alert pack is meant to close.
- **Official reference:** Loki exposes /metrics and /ready for monitoring (Grafana Loki 3.x 'Observability / monitoring Loki' docs); Prometheus target liveness is only observable for configured scrape targets (prometheus.io scrape_config docs). The self-monitoring the comment describes requires an actual job.
- **Fix:** Add scrape jobs for loki (targets: ['loki:3100']), grafana ('grafana:3000') and alloy ('alloy:12345') so 'up==0' / target-down covers them; or correct the misleading Loki comment. A Loki-specific readiness alert on 'up{job="loki"}' closes the log-pipeline blind spot.
- **Verifier:** Verified TRUE at real lines. /Users/alex/Documents/v2e-environment/v2e-compose/observability/config/prometheus.yml:14-27 defines scrape_configs for exactly prometheus, node, cadvisor, traefik — no loki/grafana/alloy job, and no /ready or :3100/:3000/:12345 target anywhere in config/. The compose.yml loki comment ("Readiness is scraped by prometheus + alerted via grafana instead (GET /ready)") is t…

### Renovate cannot see the version pins it is supposed to bump (no customManagers) · **NEW**
- **Where:** `v2e-templates/renovate.json:3-8 vs config.env:11-12,47`
- **Problem:** renovate.json only extends config:recommended, which does not scan arbitrary shell/env files. So SOPS_VERSION="3.9.4", AGE_VERSION="1.2.1", VYOS_URL/VYOS_SHA256, and the image URL/SHA pins in config.env are invisible to Renovate and will never be bumped. This directly violates the owner's rule 'pin exact versions, Renovate bumps them' — the pins will silently rot (sops is already past 3.9.4 upstream) and the config.env comment 'If upstream publishes a newer image...' is left entirely to manual toil.
- **Official reference:** Renovate docs (Custom Managers / modules/manager/regex): 'Renovate's default configuration does not automatically detect version variables in arbitrary shell/env files; you must explicitly configure custom regex managers' using managerFilePatterns + matchStrings with datasource=github-releases/docker.
- **Fix:** Add a customManagers[] regex block matching SOPS_VERSION/AGE_VERSION (datasource=github-releases, depName getsops/sops, FiloSottile/age) and the VYOS_URL/docker tag, using inline `# renovate: datasource=... depName=...` comments in config.env.
- **Verifier:** CONFIRMED. /Users/alex/Documents/v2e-environment/v2e-templates/renovate.json lines 3-8 extend only standard presets (config:recommended, group:allNonMajor, :dependencyDashboard, :semanticCommits) with no customManagers[] block. /Users/alex/Documents/v2e-environment/v2e-templates/config.env line 11 SOPS_VERSION="3.9.4", line 12 AGE_VERSION="1.2.1", lines 47-48 VYOS_URL/VYOS_SHA256, plus UBUNTU_URL/…

### Blocking CI gates pull :latest Docker images (and mix actions/checkout majors) — non-reproducible supply chain · **NEW**
- **Where:** `v2e-templates/.github/workflows/ci.yml:25,35,32`
- **Problem:** The two BLOCKING secret-scan gates run `trufflesecurity/trufflehog:latest` (line 25) and `ghcr.io/gitleaks/gitleaks:latest` (line 35) — mutable tags pulled fresh each run, so an upstream release can silently change scan behavior or break the gate that guards 'keep this repo secret-free'. Separately the workflow pins actions/checkout@v4 in two jobs (lines 12, 22) but @v7 in the gitleaks job (line 32) — inconsistent majors, non-reproducible, and the odd one out is a foot-gun if v7 regresses.
- **Official reference:** GitHub Actions security-hardening guidance recommends pinning actions/images to a specific version (ideally a commit SHA) rather than floating tags for reproducibility and supply-chain integrity; Docker best practice is to pin images by digest or an immutable tag, not :latest.
- **Fix:** Pin trufflehog/gitleaks to the exact released image tags already tracked by pre-commit (v8.21.2 for gitleaks) or a digest, and unify actions/checkout to one major across all jobs; let Renovate bump them.
- **Verifier:** Verified against /Users/alex/Documents/v2e-environment/v2e-templates/.github/workflows/ci.yml. All cited lines match exactly: line 25 `trufflesecurity/trufflehog:latest`, line 35 `ghcr.io/gitleaks/gitleaks:latest`, line 32 `actions/checkout@v7`, lines 12 and 22 `actions/checkout@v4`. Both `:latest` images run inside BLOCKING gates (secret-scan and gitleaks jobs, per the "Blocking:" comments on lin…

### CI security-scanner Docker images pinned to :latest (mutable, and invisible to Renovate) · **NEW**
- **Where:** `v2e-tf/.github/workflows/ci.yml:43,53,63; v2e-compose/.github/workflows/ci.yml:29,39,49; v2e-templates/.github/workflows/ci.yml:25,35; v2e-ansible/.github/workflows/ci.yml:35,45`
- **Problem:** Every blocking/advisory scanner is invoked as `docker run ... trufflesecurity/trufflehog:latest`, `ghcr.io/gitleaks/gitleaks:latest`, `aquasec/trivy:latest`. This is a mutable floating tag, which directly violates the owner rule 'pin exact versions'. Worse, Renovate cannot see these images: they are string literals inside `run:` shell steps, not in a Dockerfile/compose file, and no customManager is configured — so the docker-manager never bumps them and the pre-commit/renovate comment 'Renovate also tracks these' does not apply to CI scanners. Result: scan results are non-reproducible (a new :latest can change rules/exit behaviour overnight, silently breaking or weakening the 'blocking, keep repo secret-free' gate), and a poisoned upstream tag runs with your repo mounted.
- **Official reference:** GitHub Actions security hardening ('Using third-party actions'/supply-chain) and Renovate docker manager docs: pin container images to a specific tag or digest so builds are reproducible and updatable; Renovate only detects images in known managers, not arbitrary shell `docker run`.
- **Fix:** Pin each scanner to an exact version+digest (e.g. `ghcr.io/gitleaks/gitleaks:v8.21.2`, `aquasec/trivy:0.58.0`, `trufflesecurity/trufflehog:3.88.0`) and either switch to the maintained actions (gitleaks/gitleaks-action@<sha>, aquasecurity/trivy-action@<sha>) or add a Renovate customManager regex so these `docker run` pins get bumped. Aligns with the checkov job which already uses a versioned action.
- **Verifier:** Confirmed at all cited lines. v2e-tf/.github/workflows/ci.yml:43 (trufflesecurity/trufflehog:latest), :53 (ghcr.io/gitleaks/gitleaks:latest), :63 (aquasec/trivy:latest); v2e-compose ci.yml:29/39/49; v2e-templates ci.yml:25/35; v2e-ansible ci.yml:35/45 — all match exactly, each via `docker run --rm -v "$PWD:/repo" ...:latest`. The Renovate-blindness claim is verified: every repo's renovate.json is …

### Blocking secret-scan never inspects git history (shallow checkout + filesystem/--no-git mode) · **NEW**
- **Where:** `v2e-tf/.github/workflows/ci.yml:40-53 (and identical secret-scan/gitleaks jobs in v2e-compose:26-39, v2e-templates:22-35, v2e-ansible:32-45)`
- **Problem:** The two jobs advertised as 'Blocking: keep it secret-free' only scan the working tree: TruffleHog runs `filesystem /repo` and gitleaks runs `detect --no-git`. No job sets `fetch-depth: 0` (grep shows zero fetch-depth in any workflow), so checkout is shallow (depth 1) anyway. A secret that was committed and later removed in a subsequent commit passes CI cleanly — which is the exact scenario secret-history scanning exists to catch. TruffleHog additionally uses `--only-verified`, so any credential whose verifier is offline/unsupported (raw private keys, self-hosted tokens) never fails the build. The gate is materially weaker than its 'blocking' label implies.
- **Official reference:** gitleaks docs: `detect` (git mode) scans commit history; `--no-git` scans only the current tree. TruffleHog docs: use the `git` source with full clone to scan history. actions/checkout defaults to fetch-depth 1; history scanning requires fetch-depth 0.
- **Fix:** For the git-history scan add `with: { fetch-depth: 0 }` to checkout and run gitleaks in git mode (`gitleaks detect --source=/repo --redact`) and/or TruffleHog `git file:///repo`. Keep the tree/no-git pass as a supplement, not the sole check.
- **Verifier:** CONFIRMED at /Users/alex/Documents/v2e-environment/v2e-tf/.github/workflows/ci.yml lines 37-54. The secret-scan job (L37-44) runs `trufflesecurity/trufflehog:latest filesystem /repo --only-verified --fail` and the gitleaks job (L47-54) runs `gitleaks detect --source=/repo --no-git --redact`. Both are labeled "Blocking" (L36, L46). No `fetch-depth: 0` exists in any workflow — grep for "fetch-depth"…

### Arcane relies on default admin/password changed manually on first boot · _tracked_
- **Where:** `v2e-compose/arcane/compose.yml:38-39 (comment 'Default first-boot login is admin/password — change it immediately')`
- **Problem:** Confirmed against upstream: Arcane v2.3 ships default admin credentials and only prompts to change them at first sign-in; there is no env-seeded admin. Tracked as H3. The window between deploy and manual change leaves the socket-mounted (root-equiv) manager behind default creds. Arcane exposes ENCRYPTION_KEY/JWT_SECRET via SOPS already but no admin-seed variable.
- **Official reference:** Arcane installation docs (getarcane.app/docs/setup/installation): 'The first time you sign in, you'll be asked to change the default admin password.'
- **Fix:** Seed real admin creds via env/SOPS if Arcane supports it, or automate the first-boot password change; at minimum gate the UI behind TinyAuth (already done) and document the mandatory immediate change in the runbook (per H3).
- **Verifier:** Confirmed at /Users/alex/Documents/v2e-environment/v2e-compose/arcane/compose.yml. The referenced comment exists (lines ~37-38, within the labels block, slightly off from the cited 38-39): "# Gated: TinyAuth forward-auth in front of Arcane's own login. Default first-boot / # login is admin/password — change it immediately in the UI." The claim is TRUE and not already fixed: the environment block s…

### Alert delivery is not wired — six provisioned rules fire but route nowhere (no contactPoints / notification policy provisioned) · _tracked_
- **Where:** `v2e-compose/observability/config/grafana-alerting.yaml (branch) — file contains only 'groups:', no 'contactPoints:' or 'policies:'`
- **Problem:** Grafana file provisioning supports contactPoints and policies alongside groups, but only groups are provided. With no default contact point, all six rules (target-down, cert-expiry warn/crit, dns-internal-down, disk-low, mem-high) evaluate and fire silently in the Grafana UI and deliver no notification. For a single-operator homelab this means the alerting pack provides no out-of-band signal — the operator must be looking at Grafana to see a fire.
- **Official reference:** Grafana file-provisioning docs: 'Provisioning rules alone creates the conditions but not the delivery mechanism' — contactPoints and policies must be provisioned to deliver notifications.
- **Fix:** Provision a default contact point + notification policy (SOLIDIFY recommends the caronc/apprise webhook -> ntfy path; one SOPS secret for the target URL). Track under SOLIDIFY 'Apprise?'.
- **Verifier:** VERIFIED TRUE on branch feat/alerting-and-log-scrubbing (the finding's "where" explicitly says "(branch)"; main only has 1 rule, the branch is where the alerting pack lives).  File: /Users/alex/Documents/v2e-environment/v2e-compose/observability/config/grafana-alerting.yaml @ feat/alerting-and-log-scrubbing. - Confirmed exactly six rules with the named UIDs: target-down (L14), cert-expiry-warning …

### Compose CI validates only 3 of 9 stacks · _tracked_
- **Where:** `v2e-compose/.github/workflows/ci.yml:8-19 (compose job)`
- **Problem:** The compose validation job runs `docker compose config -q` only for traefik, tinyauth, whoami. The repo actually ships 9 stacks (arcane, mullvad-exit, observability, rustdesk, semaphore, technitium, tinyauth, traefik, whoami). arcane, mullvad-exit, observability, rustdesk, semaphore, technitium get no syntax/interpolation validation, so a broken compose.yml in any of them (the ones with the most env plumbing) merges green.
- **Official reference:** docker compose CLI: `docker compose config -q` validates a compose file; running it per-stack is the documented lint for daemonless CI.
- **Fix:** Loop over every `*/compose.yml` with a per-stack dummy-env matrix (or a small shell loop) so all 9 stacks are validated. Provide minimal required env vars per stack.
- **Verifier:** Confirmed at /Users/alex/Documents/v2e-environment/v2e-compose/.github/workflows/ci.yml. The `compose` job's `run` block (lines ~12-19) runs `docker compose ... config -q` only for traefik/compose.yml, tinyauth/compose.yml, and whoami/compose.yml. The repo ships exactly 9 stacks: arcane, mullvad-exit, observability, rustdesk, semaphore, technitium, tinyauth, traefik, whoami. So 6 stacks (arcane, m…


## Low

### VyOS default-deny boundary is IPv4-only; no firewall ipv6 rules configured · **NEW**
- **Where:** `v2e-tf/cloud-init/vyos-router.yaml.tftpl:59-172 (firewall ipv4 input/forward filters only)`
- **Problem:** The 'default-deny isolation boundary' only ever sets `firewall ipv4 input filter` and `firewall ipv4 forward filter`. There is no `firewall ipv6 input/forward` ruleset. In VyOS 1.4/1.5 an interface with no IPv6 firewall ruleset has NO filtering on that family, so if the WAN ever obtains an IPv6 address (RA/DHCPv6) the router's own services (SSH :22) and all inter-VLAN IPv6 forwarding are wide open, silently bypassing the entire documented boundary. The default-deny claim in the header comment is therefore only half-true.
- **Official reference:** VyOS firewall docs (docs.vyos.io/en/latest/configuration/firewall) — IPv4 and IPv6 rulesets are independent; a family with no ruleset is unfiltered. Best practice is to mirror input/forward default-drop into `firewall ipv6 ...` or disable IPv6 on the interfaces.
- **Fix:** Either add `set firewall ipv6 input filter default-action drop` + `set firewall ipv6 forward filter default-action drop` (plus lo/established-related accepts) mirroring the IPv4 rules, or explicitly disable IPv6 on eth0/eth1 (`set interfaces ethernet ethX ipv6 address no-default-link-local` / disable-forwarding) so no v6 path exists. Gate on firewall_enabled like the v4 block.
- **Verifier:** Confirmed against /Users/alex/Documents/v2e-environment/v2e-tf/cloud-init/vyos-router.yaml.tftpl. The firewall block (lines 59-172, gated on firewall_enabled) is exclusively IPv4: line 65 `set firewall ipv4 input filter default-action 'drop'` and line 91 `set firewall ipv4 forward filter default-action 'drop'`, with all rules under `firewall ipv4 ...`. There is zero `firewall ipv6` config anywhere…

### Rendered cloud-init snippets on the PVE datastore are a second plaintext copy of all key material (distinct from tfstate/C2) · **NEW**
- **Where:** `v2e-tf/nodes.tf:50-61 + router.tf:53-62 (proxmox_virtual_environment_file snippets); rendered from node.yaml.tftpl:34 (chpasswd type:text), :57 (private key), :100 (age key)`
- **Problem:** The generated user-data snippets uploaded to snippet_datastore_id ('local') contain, in plaintext: both ED25519 mesh private keys, sudo_password (chpasswd type:text), root_password, ansible_vault_password, the SOPS age PRIVATE key, and the cloudflared connector token. These files persist on the Proxmox host filesystem (/var/lib/vz/snippets) for the VM's whole lifetime and land in any datastore/host backup. HANDOVER C1/C2 track the age key in the repo and plaintext tfstate, but this on-hypervisor plaintext copy is a separate, untracked exposure surface.
- **Official reference:** bpg/proxmox virtual_environment_file docs note snippet contents are stored as-is on the node; cloud-init best practice (cloudinit.readthedocs.io) is to avoid embedding long-lived private keys/secrets in NoCloud user-data. Standard guidance: treat every rendered copy of secrets as key material.
- **Fix:** Acknowledge the PVE snippet store as sensitive alongside tfstate; restrict/rotate. Where feasible, deliver the age key / mesh key via a shorter-lived channel or ensure `tofu destroy` (which does remove these managed file resources) runs on teardown, and exclude the snippets datastore from shared backups. At minimum document it in the C2 threat model.
- **Verifier:** Confirmed at real lines. nodes.tf:50-61 and router.tf:53-62 both declare proxmox_virtual_environment_file with content_type="snippets" and datastore_id=var.snippet_datastore_id (default "local", variables.tf:43 -> /var/lib/vz/snippets on the PVE host). The rendered template node.yaml.tftpl carries plaintext key material exactly as cited: chpasswd type:text at :34-41 (sudo + root passwords), hub ED…

### Provider version constraints use `~>` ranges, contradicting the owner's exact-pin rule · **NEW**
- **Where:** `v2e-tf/versions.tf:9,13,17,21,25 (proxmox ~>0.111.0, tls ~>4.3, time ~>0.14.0, cloudflare ~>5.21, external ~>2.3)`
- **Problem:** Owner rule is 'pin exact versions; Renovate bumps them.' The required_providers use range operators. `~> 5.21` (cloudflare) allows anything up to <6.0 and `~> 4.3` (tls) up to <5.0 — and cloudflare v5 has shipped breaking schema changes mid-minor (e.g. the access-policy serialization regression in 5.3). The committed .terraform.lock.hcl pins exact versions so the normal clean-rebuild is reproducible, but `tofu init -upgrade` or a lockfile-less checkout would float within the range against the owner's stated policy. Also required_version is an unbounded `>= 1.9.0` with the tofu binary itself unpinned anywhere.
- **Official reference:** OpenTofu docs (opentofu.org/docs/language/providers/requirements) — the lockfile guarantees reproducibility, but the version constraint expresses intended allowed range; owner policy (MEMORY: pin exact, Renovate bumps) wants `= x.y.z`.
- **Fix:** Change constraints to exact `= 0.111.0`, `= 4.3.0`, `= 0.14.0`, `= 5.21.1`, `= 2.4.0` (Renovate will bump both constraint and lock together), matching the Galaxy/provider exact-pin convention used elsewhere. Optionally pin tofu via a tenv/.opentofu-version file.
- **Verifier:** CONFIRMED. File /Users/alex/Documents/v2e-environment/v2e-tf/versions.tf exists and lines match exactly: line 9 proxmox "~> 0.111.0", line 13 tls "~> 4.3", line 17 time "~> 0.14.0", line 21 cloudflare "~> 5.21", line 25 external "~> 2.3". required_version is unbounded ">= 1.9.0" (line 4). All are range/floor operators, not exact pins.  The owner policy is real and directly on point: MEMORY note pi…

### AI toolchain versions are unpinned (nodejs major-only, claude-code / codex npm globals floating) — non-reproducible agent node, violates pin-exact rule · **NEW**
- **Where:** `roles/ai_workbench/defaults/main.yml:7-10 (ai_workbench_node_major '22', ai_workbench_npm_global) + roles/ai_workbench/tasks/main.yml:36-47 (apt nodejs state=present, community.general.npm state=present)`
- **Problem:** Node.js is installed from the NodeSource node_22.x repo with `state: present` (floats to whatever 22.x is current at rebuild time), and the two agent CLIs are installed as `community.general.npm` globals with `state: present` and no version — @anthropic-ai/claude-code and @openai/codex resolve to whatever npm latest is on the rebuild day. The owner rule is 'pin exact versions; Renovate bumps them' (honored everywhere else: galaxy roles, collections, geerlingguy.docker 7.9.0, rustdesk_client_version+sha256), but the AI workbench toolchain is exempt, so two rebuilds a month apart produce different agent-node software with no lockfile.
- **Official reference:** community.general.npm module docs (the `version` parameter pins an exact release; without it state=present installs latest) + NodeSource install guidance; repo's own pin-exact policy in requirements.yml.
- **Fix:** Add an ai_workbench_node_version (full x.y.z) and per-package version pins for the npm globals (community.general.npm `version:`), defaulting to the currently-deployed releases, so the agent node rebuilds deterministically and Renovate can bump them.
- **Verifier:** Verified against the actual repo. defaults/main.yml:7 = ai_workbench_node_major "22"; lines 8-10 = ai_workbench_npm_global [@anthropic-ai/claude-code, @openai/codex] with no versions. tasks/main.yml:36-39 = apt nodejs state:present from NodeSource node_22.x repo (major pinned, minor/patch floats). tasks/main.yml:43-48 = community.general.npm global:true state:present no version:, so both CLIs reso…

### mullvad-exit stack omits no-new-privileges on both gluetun and tailscale — breaks the lab-wide invariant · **NEW**
- **Where:** `v2e-compose/mullvad-exit/compose.yml:16-41 (gluetun) and :43-63 (tailscale)`
- **Problem:** Every other stack in the repo sets `security_opt: [no-new-privileges:true]` (confirmed: traefik, tinyauth, whoami, semaphore, postgres, arcane, technitium, rustdesk, all observability). The mullvad-exit stack — added later (2 Jul 17:35) — sets neither service's security_opt. HANDOVER §5 lists 'no-new-privileges everywhere' as a verified-good control, and this stack silently violates it. These two containers run with NET_ADMIN/NET_RAW and forward untrusted VPN exit traffic, so a suid-escalation guard is exactly where it matters. no-new-privileges is fully compatible with cap_add (capabilities are granted at container start regardless; the flag only blocks later setuid/file-cap escalation).
- **Official reference:** Docker security best practice / repo's own established convention (HANDOVER.md §5 'no-new-privileges everywhere', applied on all 8 other stacks). Docker docs: `--security-opt no-new-privileges` recommended for all containers, orthogonal to cap_add.
- **Fix:** Add `security_opt: [no-new-privileges:true]` to both the gluetun and tailscale services in mullvad-exit/compose.yml.
- **Verifier:** Confirmed at /Users/alex/Documents/v2e-environment/v2e-compose/mullvad-exit/compose.yml. gluetun (lines 16-41) and tailscale (lines 43-63) both lack security_opt — grep for security_opt in that file returns NONE. All 8 other stacks (arcane, observability, rustdesk, semaphore, technitium, tinyauth, traefik, whoami) set `security_opt: [no-new-privileges:true]` (verified whoami:5-6 with exact syntax …

### mullvad-exit stack omits mem_limit/cpus on both services — drift from the A2 'caps on every stack' sweep · **NEW**
- **Where:** `v2e-compose/mullvad-exit/compose.yml:16-41 (gluetun) and :43-63 (tailscale)`
- **Problem:** The A2 code-review sweep (HANDOVER §7) explicitly landed 'mem/cpu limits + healthchecks on every stack' (v2e-compose #12). gluetun and tailscale in mullvad-exit have neither `mem_limit` nor `cpus` (grep confirms mem_limit present on all other services, absent here). An unbounded VPN/exit-node container on the infra node (2 vCPU / 2 GB, also running Technitium DNS and the RustDesk relay) can starve the foundational DNS service under load. This is pure drift — the stack was authored after the sweep and missed the policy.
- **Official reference:** Repo's own established policy (HANDOVER.md §7 A2: 'mem/cpu limits ... on every stack'). Docker Compose best practice: set resource limits on every service.
- **Fix:** Add `mem_limit` and `cpus` to both services (e.g. gluetun 128m/0.5, tailscale 128m/0.5) matching the sizing style of the other stacks.
- **Verifier:** Confirmed TRUE at /Users/alex/Documents/v2e-environment/v2e-compose/mullvad-exit/compose.yml — gluetun (lines 16-41) and tailscale (43-63) both lack mem_limit and cpus. Line ranges in the finding are accurate. grep across v2e-compose confirms mullvad-exit is the sole stack missing mem_limit; all 8 other stacks (rustdesk, tinyauth, whoami, arcane, observability, semaphore, technitium, traefik) have…

### No cap_drop: [ALL] on any core stack (only tracked for traefik/alloy/blackbox) · **NEW**
- **Where:** `v2e-compose/{tinyauth,whoami,semaphore,technitium}/compose.yml (all services) — grep shows zero cap_drop anywhere in the repo; only cap_add exists (mullvad-exit)`
- **Problem:** None of the core containers drop capabilities. SOLIDIFY.md ('Docker security') tracks explicit cap-drop ONLY for the stateless traefik/alloy/blackbox as a 'gradual follow-up' — it does not cover tinyauth, whoami, semaphore, postgres, or technitium. whoami and tinyauth in particular are trivially droppable (no privileged syscalls), and postgres/technitium/semaphore need only a small known set. Dropping ALL caps shrinks the escape blast radius, complementing the no-new-privileges already in place. Calibration: for a single-operator homelab this is defense-in-depth, so low — but it is a concrete, unlisted gap for the non-tracked services.
- **Official reference:** Docker/CIS Docker Benchmark 5.3: run containers with `--cap-drop all` and add back only what is required. Traefik/Postgres official hardening guides recommend cap_drop:[ALL] with cap_add of only NET_BIND_SERVICE / none.
- **Fix:** Add `cap_drop: [ALL]` to whoami and tinyauth (need nothing back); add `cap_drop: [ALL]` with a minimal cap_add to semaphore/postgres/technitium after a converge test.
- **Verifier:** CONFIRMED. No cap_drop in any actual compose file: grep -rn over v2e-compose hits only README.md:132 (backlog doc) and cap_add in mullvad-exit/compose.yml; every service config lacks cap_drop. no-new-privileges:true IS present (tinyauth/compose.yml, whoami/compose.yml), so the complement framing holds. SOLIDIFY.md exists on branch docs/solidify-plan (commit 70d9f87), lines 117-119 scope cap-drop O…

### cadvisor is the one service missing security_opt: no-new-privileges, contradicting the stated 'no-new-privileges everywhere' invariant · **NEW**
- **Where:** `v2e-compose/observability/compose.yml:141-164 (cadvisor service)`
- **Problem:** Every other service in this stack sets security_opt: [no-new-privileges:true] (prometheus, grafana, loki, alloy, uptime-kuma, node-exporter, and blackbox on the branch). cadvisor omits it entirely. HANDOVER §5 lists 'no-new-privileges everywhere' under 'Verified good', which is now inaccurate. cadvisor does not require privilege escalation (it reads host paths and /dev/kmsg via mounts/devices), so no-new-privileges:true is safe and recommended for it.
- **Official reference:** Docker security best practice / cadvisor deployment examples run the collector with no privilege-escalation; docker-compose security_opt no-new-privileges is the documented drop-privilege primitive and is compatible with cadvisor's read-only host mounts.
- **Fix:** Add security_opt: [no-new-privileges:true] to the cadvisor service to match the rest of the stack and restore the invariant.
- **Verifier:** Confirmed at /Users/alex/Documents/v2e-environment/v2e-compose/observability/compose.yml:141-164. The cadvisor service has no security_opt block, whereas every other service sets no-new-privileges:true (prometheus:10-11, grafana:32-33, loki:67-68, alloy:86-87, uptime-kuma:104-105, node-exporter:131-132). HANDOVER.md:115 states 'no-new-privileges everywhere' under 'Verified good', so the invariant …

### Alloy log-scrub regex misses JSON-quoted secret keys ("password": "x"), so structured-JSON logs persist secrets in Loki for the full 14d retention · **NEW**
- **Where:** `v2e-compose/observability/config/config.alloy (branch feat/alerting-and-log-scrubbing), loki.process "scrub" first stage.replace expression`
- **Problem:** The key=value stage matches '(password|token|...)(\s*[:=]\s*)(value)'. Between the key and the separator it only allows \s*, so a JSON log line like {"password": "hunter2"} or {"token":"abc"} is NOT redacted — the closing quote of the JSON key ('password"') sits between the keyword and the colon and breaks the match. Many containers here log structured JSON (Grafana, Traefik, Loki, Alloy itself), so the scrub — a defense-in-depth control added specifically to keep credentials out of Loki — has a real coverage hole for the most common modern log format.
- **Official reference:** Grafana Alloy loki.process stage.replace docs (RE2 regex, in-flight rewrite). RE2 supports an optional quote in the separator class; the pattern simply needs to allow a trailing quote after the key and a leading quote before the value.
- **Fix:** Broaden the separator group to tolerate an optional closing quote on the key, e.g. (key)("?\s*[:=]\s*)(?:"[^"]*"|'[^']*'|[^\s,}]+). Add a test log line in JSON form to confirm redaction.
- **Verifier:** Confirmed at v2e-compose/observability/config/config.alloy:44 on branch feat/alerting-and-log-scrubbing (not on main). The scrub stage.replace separator group is (\s*[:=]\s*), which only permits whitespace between the keyword and the colon/equals. For a JSON log line like {"password": "hunter2"} or {"token":"abc"}, the closing quote of the JSON key sits immediately after the keyword, so \s*[:=] fa…

### Renovate pre-commit manager is disabled by default — comment claims it tracks the hook pins but it does not · **NEW**
- **Where:** `v2e-templates/.pre-commit-config.yaml:3 (comment) and renovate.json (no :enablePreCommit)`
- **Problem:** .pre-commit-config.yaml states 'Bump pins with pre-commit autoupdate (Renovate also tracks these).' But renovate.json does not enable the pre-commit manager, and that manager is disabled by default. So the pinned hook revs (pre-commit-hooks v5.0.0, shellcheck v0.10.0, gitleaks v8.21.2) are NOT tracked by Renovate — the comment is false and the security-scanning hooks will drift stale.
- **Official reference:** Renovate docs (modules/manager/pre-commit): 'the pre-commit manager is disabled by default and requires explicit opt-in' via `"pre-commit": {"enabled": true}` or `:enablePreCommit` in extends.
- **Fix:** Add `:enablePreCommit` to the extends array in renovate.json (or set pre-commit.enabled=true), or fix the comment to stop claiming Renovate tracks them.
- **Verifier:** Confirmed at actual files. v2e-templates/.pre-commit-config.yaml line 2 states "Bump pins with: pre-commit autoupdate (Renovate also tracks these)." v2e-templates/renovate.json extends only [config:recommended, group:allNonMajor, :dependencyDashboard, :semanticCommits] with no :enablePreCommit and no "pre-commit":{"enabled":true}. Renovate's pre-commit manager is disabled by default (official docs…

### Cloud-image sha256 pins are trust-on-first-use, never tied to the distro's signed checksums · **NEW**
- **Where:** `v2e-templates/config.env:22,27,34 + lib/build-template.sh:26,34`
- **Problem:** The build verifies each base image against a hardcoded sha256 in config.env, but nothing documents or enforces that that sha came from Ubuntu/Debian/Parrot's GPG-signed checksum manifest. If the pinned value was ever copied from a MITM'd page (or an attacker gets a PR merged bumping URL+sha together), the check passes against a malicious image. It is pure TOFU with no provenance link. (Calibration: for a single-operator homelab, one-time manual GPG verification is a reasonable middle ground — full automated GPG-in-CI is likely overkill; the gap is that even the manual step is undocumented.)
- **Official reference:** Ubuntu cloud-images and Debian cloud publish SHA256SUMS with a detached GPG signature (SHA256SUMS.gpg) signed by the distro image-signing key; upstream verification guidance is to check the signature on the checksum file before trusting the image hash.
- **Fix:** Document (or script) fetching SHA256SUMS + SHA256SUMS.gpg and verifying the GPG signature when first pinning/refreshing each *_SHA256 in config.env, so the pin's provenance is auditable.
- **Verifier:** Confirmed at real lines. config.env:22/27/34 are UBUNTU_SHA256/DEBIAN_SHA256/PARROT_SHA256 pins (URLs sit on 21/26/33); lib/build-template.sh:26 and :34 are the two `sha256sum -c` verify points (cached image + downloaded .part). Repo-wide grep for gpg|SHA256SUMS|signature|signed returns zero hits, so the claim that provenance is undocumented/unenforced is TRUE and not already fixed — the only guid…

### No GITHUB_TOKEN permissions block on any ci.yml (default token scope, not least-privilege) · **NEW**
- **Where:** `v2e-tf/.github/workflows/ci.yml (top), v2e-compose/.github/workflows/ci.yml, v2e-templates/.github/workflows/ci.yml, v2e-ansible/.github/workflows/ci.yml — none declare `permissions:`; only v2e-docs/docs.yml does`
- **Problem:** The four ci.yml workflows omit a top-level `permissions:` block, so GITHUB_TOKEN inherits the repository/organization default. If the default is left at 'read and write', any of the many third-party actions or `:latest` scanner containers these workflows pull runs with a write-capable token on public repos. docs.yml already models the correct pattern (`permissions: contents: read` + scoped pages/id-token), so the omission in the CI workflows is an inconsistency, not a knowledge gap.
- **Official reference:** GitHub Docs, 'Automatic token authentication' / 'Security hardening for GitHub Actions': set the minimum required permissions (default `contents: read`) at the workflow level rather than relying on the repository default.
- **Fix:** Add `permissions:\n  contents: read` at the top of each ci.yml (these jobs only read the repo). Grant per-job write scopes only where actually needed.
- **Verifier:** Verified against actual repo files. All four workflows lack a top-level permissions block: v2e-tf/.github/workflows/ci.yml (no permissions; jobs start at line 7 `jobs:`, first job `tofu` at line 8), v2e-compose/.github/workflows/ci.yml, v2e-templates/.github/workflows/ci.yml, v2e-ansible/.github/workflows/ci.yml — grep for "permissions" returns nothing in all four. By contrast v2e-docs/.github/wor…

### GitHub Actions pinned to movable major tags, not commit SHAs; Renovate not configured to digest-pin · **NEW**
- **Where:** `all */.github/workflows/*.yml (e.g. actions/checkout@v7, opentofu/setup-opentofu@v2, bridgecrewio/checkov-action@v12, terraform-linters/setup-tflint@v4, actions/setup-python@v5, actions/deploy-pages@v4); renovate.json in all four repos extends only config:recommended`
- **Problem:** Every `uses:` is a movable tag. The owner rule is 'pin exact versions; Renovate bumps them', but `config:recommended` does NOT include `helpers:pinGitHubActionDigests`, so Renovate leaves tags as tags and never pins to SHA — the stated pin-exact rule is unmet for the Actions supply chain. A retagged/compromised action (including first-party) would be pulled silently. Calibrated for a single-operator homelab this is lower urgency than the runtime estate, but it is a concrete deviation from both GitHub's hardening guidance and the repo's own pinning doctrine.
- **Official reference:** GitHub 'Security hardening for GitHub Actions' — pin actions to a full-length commit SHA; Renovate docs — add `helpers:pinGitHubActionDigests` to convert tags to SHA pins (config:recommended does not enable it by default).
- **Fix:** Add `helpers:pinGitHubActionDigests` to the `extends` array in each renovate.json and let the first Renovate run convert all `uses:` to `<sha> # vX`; Renovate then maintains them.
- **Verifier:** Verified. All four renovate.json (v2e-tf, v2e-compose, v2e-templates, v2e-ansible) extend config:recommended + group:allNonMajor + :dependencyDashboard + :semanticCommits; none include helpers:pinGitHubActionDigests. Every workflow `uses:` is a movable major tag, confirmed at cited lines: v2e-tf/.github/workflows/ci.yml (checkout@v7 L12/29/40/50/60/70, opentofu/setup-opentofu@v2 L13, bridgecrewio/…

### v2e-docs has no renovate.json, so its pinned deps (mkdocs-material, actions) go stale forever · **NEW**
- **Where:** `v2e-docs/.github/workflows/docs.yml:29 (`pip install mkdocs-material==9.7.6` with comment 'Pinned exact; Renovate bumps it') and lines 21/23/33/40 (checkout@v4, setup-python@v5, upload-pages-artifact@v3, deploy-pages@v4)`
- **Problem:** The docs workflow comment claims Renovate maintains the mkdocs-material pin, but v2e-docs is the one repo with no renovate.json (present in the other four). Nothing bumps mkdocs-material==9.7.6 or the pinned actions here — the pins silently rot and the comment is inaccurate.
- **Official reference:** Renovate requires a config (renovate.json) in each repo to run; without it the pip/actions pins are never updated.
- **Fix:** Add the same renovate.json (config:recommended + group:allNonMajor + dependencyDashboard + semanticCommits) to v2e-docs so the pip_requirements/github-actions managers cover docs.yml.
- **Verifier:** TRUE and not already fixed. Verified /Users/alex/Documents/v2e-environment/v2e-docs/.github/workflows/docs.yml: line 27 comment "Pinned exact; Renovate bumps it." sits above line 28 `pip install mkdocs-material==9.7.6`, with pinned actions checkout@v4 (L21), setup-python@v5 (L24), upload-pages-artifact@v3 (L31), deploy-pages@v4 (L43). Confirmed v2e-docs has NO renovate.json (find returned nothing)…

### No IaC misconfig gate is enforcing (checkov double-advisory; trivy/tflint/lint all soft) · **NEW**
- **Where:** `v2e-tf/.github/workflows/ci.yml:22-38 (checkov: `continue-on-error: true` AND `soft_fail: true`), :56-64 (trivy `--exit-code 0`), :66-72 (tflint `|| echo`); v2e-compose trivy:41-49; v2e-ansible lint:26-31`
- **Problem:** Every misconfig/lint scanner is non-blocking by design (documented 'flip once backlog clear'). checkov is redundantly gated twice (continue-on-error + soft_fail), and trivy is hardcoded `--exit-code 0`. So no misconfiguration finding can ever fail CI; only the two secret scanners block. This is a defensible homelab choice, but combined with the fact that branch-protection required-checks cannot be confirmed from the repo, the practical enforcement surface is just secret-scan+gitleaks — flagging so the 'advisory forever' state is a conscious decision, not forgotten.
- **Official reference:** checkov-action docs: `soft_fail: true` never fails the build; Trivy docs: `--exit-code 1` is required to gate. GitHub: advisory jobs only block merges if set as required status checks in branch protection.
- **Fix:** Leave as advisory if intended, but drop the redundant checkov continue-on-error (soft_fail already covers it), and pick a minimal set of misconfig checks to make blocking (or document explicitly in HANDOVER that CI is advisory-only and merges rely on the operator).
- **Verifier:** Verified against all three actual CI files. v2e-tf/.github/workflows/ci.yml: checkov has BOTH continue-on-error:true (line 27) and soft_fail:true (line 34) — the redundant double-gate is real; trivy --exit-code 0 hardcoded (line 64); tflint `|| echo` advisory (line 73). The "flip soft_fail/drop guards once backlog clear" comments are present verbatim (tf:23-24, ansible:18-19). Only secret-scan (Tr…

### Agent 'zero-trust' egress is a PORT allowlist, not a destination allowlist — the contained AI node can reach any internet host on 80/443 (and any host on NTP/123) · **NEW**
- **Where:** `v2e-tf/cloud-init/vyos-router.yaml.tftpl rule 26 (agent->NTP: udp 123 to ANY dest) and rule 27 (agent->allowlisted TCP: to ANY dest on AGENT-EGRESS-TCP ports); default `agent_egress_allow_tcp_ports = [80,443]` at v2e-tf/variables.tf:222 and terraform.tfvars.example:79`
- **Problem:** The firewall restricts the agent by destination port only. With the default [80,443], a misbehaving/compromised AI process on the agent VLAN can open HTTPS to arbitrary internet endpoints — i.e. an unrestricted exfiltration/C2 channel — while the config and HANDOVER describe this as 'deny-by-default / zero-trust for the AI VLAN'. DNS is destination-scoped (AGENT-DNS group) but NTP (rule 26) has no destination restriction either. The restriction stops odd ports, not data egress, so it does not deliver the containment the AI-node design implies.
- **Official reference:** VyOS firewall supports destination address-group / domain-group matching (set firewall group address-group / domain-group), the documented way to constrain egress to specific destinations. VyOS 1.4 firewall-groups docs.
- **Fix:** If real containment is intended, pin agent egress to a destination allowlist (address-group / VyOS domain-group of the endpoints the agent legitimately needs, e.g. Anthropic/OpenAI API, apt/ghcr mirrors) rather than to ports, and scope rule 26 NTP to the configured name_servers/NTP hosts. Otherwise document that 'restricted' means port-scoped, not exfil-proof.
- **Verifier:** Confirmed at real file:lines. v2e-tf/cloud-init/vyos-router.yaml.tftpl rule 27 (lines 129-134) matches only source=agent_subnet + destination port-group AGENT-EGRESS-TCP + protocol tcp, with NO destination address/address-group — a pure port allowlist to ANY internet host. Rule 26 NTP (lines 123-128) is destination port 123/udp to ANY dest, unscoped. By contrast rule 25 DNS (lines 116-122) IS dest…

### Tailscale `--ssh` on control + infra is a third SSH ingress that bypasses the repo's entire sshd hardening (key-only, AllowUsers, fail2ban) · **NEW**
- **Where:** `v2e-ansible/roles/tailscale/defaults/main.yml (tailscale_node_ssh: true) applied on control and infra via inventory/group_vars; brings up `tailscale up --ssh``
- **Problem:** Tailscale SSH terminates connections inside tailscaled using tailnet identity + tailnet ACLs, not sshd — so it is not subject to ssh_allow_users, key-only auth, or the fail2ban sshd jail this repo invests in. On a default personal tailnet the ACL is allow-all, meaning any device enrolled with the reusable authkey (which is in SOPS and reused for the mullvad-exit container identity) gets a shell on control as any local user (incl. root if permitted). HANDOVER M1 enumerates only the DNAT:2201 and Cloudflare-tunnel SSH paths to control; this third path and its bypass of the sshd controls is not tracked.
- **Official reference:** Tailscale SSH docs: connections are authenticated/authorized by tailnet ACL `ssh` rules and handled by tailscaled, independent of the host's sshd config; default ACL for a new tailnet permits all.
- **Fix:** Add explicit tailnet ACL `ssh` rules (deny by default, allow only the operator's device to specific users) and treat it as a variable-driven decision; or set tailscale_node_ssh: false where the DNAT/tunnel paths already cover access. Document the path in the M1 SSH-ingress inventory.
- **Verifier:** Verified at real file:lines. tailscale_node_ssh: true at roles/tailscale/defaults/main.yml:6 and re-set for both groups at inventory/group_vars/control.yml:10 and infra.yml:50; --ssh composed into `tailscale up` at roles/tailscale/tasks/main.yml:16 via artis3n.tailscale. sshd hardening the path bypasses is real (roles/fail2ban/tasks/main.yml sshd jail + devsec.hardening/AllowUsers). Reusable authk…

### blackbox-exporter adds cap NET_RAW without cap_drop: ALL — keeps the full default capability set plus raw sockets · _tracked_
- **Where:** `v2e-compose/observability/compose.yml (branch feat/alerting-and-log-scrubbing), blackbox-exporter service cap_add: [NET_RAW]`
- **Problem:** Upstream blackbox guidance is cap_drop: ALL then cap_add: NET_RAW for the ICMP prober; here NET_RAW is added on top of Docker's default capability set with no drop, and the ICMP module that needs it is currently gated/commented out in prometheus.yml, so the raw-socket cap is granted but unused today.
- **Official reference:** prometheus/blackbox_exporter README docker/ICMP guidance: drop all capabilities and add only NET_RAW.
- **Fix:** Add cap_drop: [ALL] alongside cap_add: [NET_RAW] (and drop NET_RAW until the tailnet ICMP job is enabled). Already tracked as the 'explicit cap-drop on stateless services (traefik, alloy, blackbox)' follow-up in SOLIDIFY.
- **Verifier:** Confirmed at v2e-compose/observability/compose.yml:132-142 (branch feat/alerting-and-log-scrubbing): blackbox-exporter has cap_add: [NET_RAW] with no cap_drop in the service block (only no-new-privileges + mem/cpu limits). Claim is TRUE and unfixed. The NET_RAW-consuming ICMP job is gated: prometheus.yml lines 60-68 (blackbox-tailnet, module icmp_node) are commented out; only blackbox-cert (HTTP) …


## Nits

### proxmox_insecure defaults to true — API token sent over an unverified TLS channel by default · **NEW**
- **Where:** `v2e-tf/variables.tf:17-21 (default = true) consumed at providers.tf:3`
- **Problem:** The provider defaults to skipping TLS verification, so the sensitive Proxmox API token (and the SSH snippet uploads) traverse a channel with no cert validation; a MITM on the management path could capture the token. This is insecure-by-default rather than opt-in-insecure. For a single-operator homelab on a trusted LAN with a self-signed PVE cert this is an acceptable trade-off, but the default inverts the secure posture.
- **Official reference:** bpg/proxmox provider docs (registry.terraform.io/providers/bpg/proxmox) — `insecure` should be enabled deliberately; recommended path is trusting the PVE CA / pinning the cert.
- **Fix:** Leave the escape hatch but flip the default to false (or document that operators should add the PVE CA to the trust store). Calibrated: low priority for this threat model; note it rather than block.
- **Verifier:** Verified against actual files. v2e-tf/variables.tf lines 17-21 define `proxmox_insecure` with `default = true` (description: "Skip TLS verification (self-signed certs)."). Consumed at v2e-tf/providers.tf line 3 as `insecure = var.proxmox_insecure`, in the same provider block that carries `api_token` and the SSH snippet upload. So the sensitive API token does traverse a TLS-unverified channel by de…

### Unattended-access secrets passed as CLI arguments leak into the target host's process table despite no_log · **NEW**
- **Where:** `roles/rustdesk_client/tasks/main.yml:45 (rustdesk --password {{...}}) and roles/ai_workbench/tasks/main.yml:70 (codex login --api-key {{...}})`
- **Problem:** Both tasks carry no_log:true (good — hides the value from Ansible output), but the secret is still passed as a command-line argument, so while the command runs it is visible in `/proc/<pid>/cmdline` and to any `ps` on the target (control for the RustDesk password, agent for the OpenAI key). no_log does nothing about on-host process-table exposure. Low impact for a single-operator homelab with throwaway creds, but it undercuts the otherwise-careful secret handling (SOPS, env.j2 $$-escape, no_log everywhere).
- **Official reference:** Ansible security docs — 'no_log' only suppresses logging/output, not on-target process visibility; recommended pattern is to pass secrets via stdin/env/file rather than argv for commands that expose them.
- **Fix:** Where the tool supports it, feed the secret via an environment variable or a 0600 temp file / stdin instead of argv (e.g. an env var for `codex login`), or accept the residual exposure as a documented homelab trade-off.
- **Verifier:** Verified against the actual repo. Both file:line references are exact: roles/rustdesk_client/tasks/main.yml:45 runs `ansible.builtin.command` with `cmd: rustdesk --password {{ rustdesk_unattended_password | quote }}` (no_log:true at :49), and roles/ai_workbench/tasks/main.yml:70 runs `cmd: "codex login --api-key {{ ai_workbench_openai_api_key }}"` (no_log:true at :74). Claim is TRUE: both secrets …

### prometheus.yml comment claims v3 'deprecates the --storage.tsdb.retention.* flags' — inaccurate; the flags remain valid · **NEW**
- **Where:** `v2e-compose/observability/config/prometheus.yml:1-3 (header comment)`
- **Problem:** Config-file retention (storage.tsdb.retention.time/size/percentage) was ADDED in Prometheus v3.12.0 and works in the pinned v3.13.0 — so the retention block itself is correct. But the CLI flags were not deprecated or removed; per prometheus PR #18200 the CLI flag values are retained as a fallback when retention is absent from the config file. The parenthetical 'deprecates the flags' could mislead a future operator into thinking the flags are gone.
- **Official reference:** Prometheus v3.12.0/v3.13.0 release notes (config-file retention feature) and prometheus/prometheus #18200 (fall back to CLI flag values when retention removed from config) — flags coexist, not deprecated.
- **Fix:** Reword the comment to 'v3 allows retention in the config file (flags still valid as fallback)'. Comment-only; no functional change.
- **Verifier:** Confirmed at v2e-compose/observability/config/prometheus.yml:1-2. The header comment literally says "v3 deprecates the --storage.tsdb.retention.* flags", which is factually wrong: the CLI flags were not deprecated. PR #18200 (real, verified) retains the CLI flag values as a fallback when retention is absent from the config file, so flags and config-file retention coexist. The retention block itsel…

### Inconsistent checkout pinning within v2e-templates/ci.yml (v4 in two jobs, v7 in a third) · **NEW**
- **Where:** `v2e-templates/.github/workflows/ci.yml:12 & 22 (actions/checkout@v4) vs :32 (actions/checkout@v7)`
- **Problem:** Within a single workflow the shellcheck and secret-scan jobs use actions/checkout@v4 while the gitleaks job uses @v7. v2e-docs also sits on @v4 while the other three repos use @v7. Harmless functionally but signals copy-paste drift and undercuts the 'pin exact / consistent' hygiene the repo aims for; a reader can't tell which is intentional.
- **Official reference:** General workflow hygiene — a repo should reference one deliberate version of a first-party action; GitHub recommends consistent, reviewed action versions.
- **Fix:** Normalise all checkout references to one version (SHA-pinned per the finding above) across every job and repo.
- **Verifier:** Verified in /Users/alex/Documents/v2e-environment/v2e-templates/.github/workflows/ci.yml: line 12 (shellcheck) and line 22 (secret-scan) use actions/checkout@v4, while line 32 (gitleaks) uses actions/checkout@v7. The intra-file inconsistency is exactly as claimed, real, and unfixed — unambiguous drift within a single workflow, not a deliberate difference. Correctly rated a nit. Official_ref is gen…
